name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install MoonBit (placeholder)
        run: |
          echo "Ensure MoonBit toolchain is available on runner"
          # If you have a setup script, run it here.
      - name: Run moon info
        run: moon info || true
      - name: Run moon check
        run: moon check
      - name: Run tests
        run: moon test --target native


